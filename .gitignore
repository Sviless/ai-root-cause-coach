# Environment / secrets — never commit real keys
.env
.env.*
!.env.example

# Streamlit secrets — never commit; keep config.toml tracked
.streamlit/secrets.toml

# Local database and exported files
data/*.db
outputs/*
!outputs/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
